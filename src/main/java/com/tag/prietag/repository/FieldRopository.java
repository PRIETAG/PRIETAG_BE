package com.tag.prietag.repository;

import com.tag.prietag.model.Field;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FieldRopository extends JpaRepository<Field, Long> {
}
